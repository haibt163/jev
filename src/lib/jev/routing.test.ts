/** Tests moved to `usecases/support-router/normalize.test.ts` and
 * `core/parse.test.ts`. Placeholder kept because this environment blocks
 * file deletion. Run `npm run test` for the current suite. */
import { describe, it } from "node:test";

describe("moved", () => {
  it("tests now live under core/ and usecases/", () => {});
});
