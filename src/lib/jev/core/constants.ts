/** System One model used for all Jev evaluations. */
export const JEV_MODEL = "jev-latest";

/** Maximum characters accepted for a single text input. */
export const MAX_MESSAGE_LENGTH = 8000;

/** Default threshold at which a Noul probability counts as "true". */
export const NOUL_TRUE_THRESHOLD = 0.5;

/** Maximum number of items in a single batch evaluation request. */
export const MAX_BATCH_SIZE = 20;

/** Human-readable prefix used for user-facing failure messages. */
export const JEV_UNAVAILABLE = "Jev evaluation unavailable.";
